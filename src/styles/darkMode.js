import { createTheme } from "@mui/material";

const darkTheme = createTheme({
    palette:{
        type: 'dark',
        primary: {
            main: "#70face",
        },
        secondary: {
            main: "#011d33",
        }
    }
});

export default darkTheme;